#include "FightController.hpp"

namespace FightClub {
    namespace Server {
        namespace Fight {

            json FightController::registerToSearch(const json& request)
            {
                
            }

        }
    }
}